# wiki-desktop

Wazoo Wiki deno desktop app — from-scratch Deno desktop boilerplate.

This is the empty starting point for the Deno desktop app, built from zero
with reference to the Deno runtime desktop docs:

- https://docs.deno.com/runtime/desktop/

Next step: add `deno.json` and `src/main.ts` on a feature branch.
